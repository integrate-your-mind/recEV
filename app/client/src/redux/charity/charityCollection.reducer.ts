import ICharityItem from '../../models/business/charity';

export type CharityCollectionState = ICharityItem[];

export const charityCollectionReducer = (state: CharityCollectionState = [], action: unknown) => {
  return state;
};
